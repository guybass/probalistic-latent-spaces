# Paper figure and table notes

`inspect-paper` found no referenced external figure assets and no result-figure
environment in the current LaTeX source. Consequently, exact pixel matching is
not an acceptance mode for this replication.

The numerical display at `main.tex:1380-1394` is an inline four-column table:

- rows: initialization and step 143000;
- reported quantities: observed minimum-eigenvalue range and the worst inverse,
  inverse-square, and inverse-cube factors;
- scale: scientific notation, no uncertainty intervals;
- interpretation: a chart-dependent pointwise conditioning diagnostic, not a
  uniform pathwise certificate.

This display maps to `PGA-08` and uses numeric equivalence. The remaining table
artifacts are audit summaries created by this replication and compared to exact
identities or explicit proof obligations, not to paper artwork.
